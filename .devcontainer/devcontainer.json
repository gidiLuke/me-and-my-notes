{
  "name": "personal-website",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:4-24-bookworm",
  "postCreateCommand": "npm install -g npm@latest && npm install && git config --global gpg.format ssh && git config --global gpg.ssh.defaultKeyCommand \"ssh-add -L\" && git config --global commit.gpgsign true",
  "customizations": {
    "vscode": {
      "settings": {
        "files.eol": "\n",
        "editor.formatOnSave": false
      },
      "extensions": [
        "dbaeumer.vscode-eslint",
        "openai.chatgpt",
        "prettier.prettier-vscode",
        "mhutchie.git-graph",
        "fnando.linter"
      ]
    }
  },
  "remoteUser": "node"
}
