{
  "name": "personal-website",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:1-20-bookworm",
  "postCreateCommand": "npm install && git config --global gpg.format ssh && git config --global gpg.ssh.defaultKeyCommand \"ssh-add -L\" && git config --global commit.gpgsign true",
  "customizations": {
    "vscode": {
      "settings": {
        "files.eol": "\n",
        "editor.formatOnSave": false
      },
      "extensions": [
        "dbaeumer.vscode-eslint"
      ]
    }
  },
  "remoteUser": "node"
}
